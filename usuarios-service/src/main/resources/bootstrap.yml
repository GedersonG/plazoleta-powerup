server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usuariosdb
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Usuarios Service by Pragma Powerup"
appVersion: "1.0.0"
